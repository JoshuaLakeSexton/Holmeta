{
  "name": "@holmeta/extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node -e \"console.log('Load unpacked extension from apps/extension')\"",
    "build": "rm -rf dist holmeta-extension.zip && mkdir -p dist/extension dist/extension/packages/shared/styles && cp manifest.json dist/extension/ && cp -R src dist/extension/ && cp -R ../../packages/shared/styles/. dist/extension/packages/shared/styles/ && if [ -d src/assets ]; then cp -R src/assets dist/extension/assets; fi && (cd dist && zip -qr ../holmeta-extension.zip extension)",
    "build:zip": "npm run build",
    "zip": "npm run build",
    "lint:palette": "node scripts/lint-palette.mjs",
    "lint": "npm run lint:palette",
    "typecheck": "node -e \"console.log('extension typecheck: no-op (plain JS)')\"",
    "test": "node -e \"console.log('extension test: smoke-only')\""
  }
}

{
  "name": "@holmeta/extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node -e \"console.log('Load unpacked extension from apps/extension')\"",
    "build": "rm -rf dist holmeta-extension.zip && mkdir -p dist/extension && rsync -a --delete --exclude 'dist' --exclude 'node_modules' --exclude '.git' --exclude 'package*.json' --exclude 'README.md' --exclude 'STORE_REVIEW.md' --exclude 'UI_ISSUES.md' --exclude 'backup' ./ dist/extension/ && (cd dist && zip -qr ../holmeta-extension.zip extension)",
    "build:zip": "npm run build",
    "zip": "npm run build",
    "check": "npm run lint && npm run typecheck",
    "lint": "node -e \"console.log('lint: no-op for backup extension layout')\"",
    "typecheck": "node -e \"console.log('extension typecheck: no-op (plain JS)')\"",
    "test": "node -e \"console.log('test: no automated tests configured for backup extension layout')\""
  }
}
